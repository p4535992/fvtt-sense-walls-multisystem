const CONSTANTS = {
  MODULE_NAME: 'sense-walls-multisystem',
  PATH: `modules/sense-walls-multisystem/`,
  FLAG_NAME: 'data',
  DEFAULTS: {
    enabled: true,
  },
};

CONSTANTS.PATH = `modules/${CONSTANTS.MODULE_NAME}/`;

export default CONSTANTS;
